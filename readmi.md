To open dashboard you'll need to open terminal then type there 
python3 -m streamlit run "app(1).py" 
python3 -m streamlit run "app(2).py" 
python3 -m streamlit run "app(3).py"

Note: it must be wrapped in "" otherwise zsh would see it as an error. 